- name: "Reload systemd and restart cart"
  tags:
    - deployment
  ansible.builtin.systemd:
    name: cart
    daemon_reload: yes
    state: restarted #why we are restaring if its running it will restart.
    enabled: yes
