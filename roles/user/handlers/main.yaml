- name: "Reload systemd and restart user"
  tags:
    - deployment
  ansible.builtin.systemd:
    name: user
    daemon_reload: yes
    state: restarted #why we are restaring if its running it will restart.
    enabled: yes
